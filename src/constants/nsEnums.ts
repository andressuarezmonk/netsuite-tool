export enum NSApprovalStatus {
  Approved = "T",
}

export enum NSSubmittedStatus {
  Submitted = "3",
}

/** Fallback item ID used when the NS session hasn't returned a real value yet. */
export const DEFAULT_ITEM_ID = "754";
