// Background service worker — no React, just message passing
// (Currently unused since content script fetches directly with credentials: 'include')
// Kept as a stub for future use (e.g. popup → background → NS API calls)

export {};
